<li class="list-group-item">
	{{burger_name}}

		{{#if isDevoured}}
		{{else}}
			<button type="button" class="devoured btn btn-outline btn-block" data-id="{{id}}" data-isDevoured="{{isDevoured}}">
				Eat this burger
			</button>
		{{/if}}

</li>